import React from 'react';
import { connect } from 'react-redux';

import { clap, getArticle } from '../redux/actions';

export default function Articles ({
  text,
  claps,
  title,
  feature_img,
  author
} = {}) {
  
}
